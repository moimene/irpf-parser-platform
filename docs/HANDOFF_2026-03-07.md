# Handoff 2026-03-07

## Objetivo de este documento

Servir como punto de reinicio para abrir un hilo nuevo sin perder contexto tecnico ni operativo.

## Estado actual resumido

- Produccion web activa en `https://web-tan-mu-35.vercel.app`
- Produccion parser activa en `https://parser-production-0827.up.railway.app`
- Parser verificado en `v0.2.0`
- Auth real desplegado sobre Supabase Auth
- Perfiles persistentes de despacho operativos
- Compatibilidad runtime activa con schema legacy de Supabase
- E2E productivo en verde: `3 passed`

## Cambios importantes ya consolidados

### 1. Flujo critico estabilizado

El flujo `upload -> intake -> review -> export -> download` ya funciona contra produccion y esta cubierto por E2E real.

### 2. Auth real de despacho

La web ya no opera con sesion simulada como mecanismo principal. Ahora usa:

- Supabase Auth para login,
- middleware de proteccion,
- resolucion de perfil persistente,
- permisos por rol,
- y scope por cliente/expediente.

Archivos clave:

- `apps/web/lib/auth.ts`
- `apps/web/lib/supabase-auth.ts`
- `apps/web/lib/supabase-auth-server.ts`
- `apps/web/middleware.ts`
- `apps/web/components/login-form.tsx`

### 3. Operativa de despacho ya iniciada

Ya existen:

- modulo de clientes,
- ficha de cliente,
- creacion de expedientes,
- admin de acceso en `Configuracion`,
- y asignaciones cliente-usuario.

Archivos clave:

- `apps/web/app/clientes/page.tsx`
- `apps/web/app/clientes/[id]/page.tsx`
- `apps/web/app/configuracion/page.tsx`
- `apps/web/components/clients-workspace.tsx`
- `apps/web/components/client-profile.tsx`
- `apps/web/components/access-admin-workspace.tsx`

### 4. Riesgo multi-cliente resuelto

El expediente ya no depende del caso trivial de "un solo cliente accesible". Si un expediente aun no esta vinculado, la UI exige seleccion explicita de cliente antes de intake.

Archivos clave:

- `apps/web/components/intake-form.tsx`
- `apps/web/app/api/documents/upload-urls/route.ts`
- `apps/web/app/api/documents/intake/route.ts`

## Realidad de schema que condiciona el siguiente trabajo

El repo ya soporta un schema moderno, pero la produccion actual sigue operando sobre tablas legacy para acceso:

- legacy: `irpf_abogados`, `irpf_asignaciones`
- moderno local: `irpf_users`, `irpf_user_client_assignments`

La compatibilidad esta resuelta en:

- `apps/web/lib/access-store.ts`
- `apps/web/lib/client-store.ts`

Esto es funcional, pero es deuda tecnica. El siguiente trabajo de auth debe partir de esta realidad.

## Riesgos y deuda viva

1. Aun no existe una migracion aplicada en produccion que alinee completamente perfiles y asignaciones con el schema moderno.
2. No existe una vinculacion fuerte y persistida `auth.users.id -> perfil de despacho` como contrato definitivo.
3. El parser sigue limitado a `Pictet`, `Goldman Sachs` y `Citi`.
4. `714` y `720` siguen siendo export base, no solucion de dominio completa.
5. No hay SSO corporativo ni gobierno avanzado de acceso.

## Siguiente backlog recomendado

### Prioridad 1. Cerrar auth de despacho

- Introducir `auth_user_id` persistente en el perfil de usuario.
- Aplicar migracion de datos desde `irpf_abogados` a `irpf_users`.
- Aplicar migracion de `irpf_asignaciones` a `irpf_user_client_assignments`.
- Retirar fallback legacy una vez migrado.
- Añadir flujo de invitacion / alta segura / reset de password.

### Prioridad 2. Endurecer administracion

- Gestion de usuarios activos/inactivos con reglas de negocio claras.
- Mejoras de UX en `Configuracion`.
- Auditoria funcional de cambios de acceso.

### Prioridad 3. Entrar en Fase 2 IRPF

- Lotes de adquisicion en runtime.
- FIFO fiscal completo.
- Ajustes manuales y trazabilidad de calculo.

## Comprobaciones utiles al arrancar un hilo nuevo

- `npm run typecheck --workspace apps/web`
- `npm run lint --workspace apps/web`
- `npm run build --workspace apps/web`
- `E2E_BASE_URL=https://web-tan-mu-35.vercel.app npm run test:e2e --workspace apps/web`

## Prompt recomendado para el siguiente hilo

`Tomamos como fuente de verdad docs/HANDOFF_2026-03-07.md y docs/BASELINE_FUNCIONAL_2026-03-06.md. Quiero cerrar la migracion de auth real de despacho: auth_user_id persistente, migracion desde irpf_abogados/irpf_asignaciones al schema moderno, y retirada del fallback legacy con tests y despliegue.`

## Actualizacion 2026-03-07 auth despacho

- Migracion aplicada en Supabase: `infra/supabase/migrations/20260307100000_finalize_dispatch_auth_migration.sql`.
- `irpf_users` queda con columna persistente `auth_user_id` y backfill desde `metadata` y `auth.users`.
- `irpf_abogados` e `irpf_asignaciones` quedan migradas a `irpf_users` e `irpf_user_client_assignments`.
- `irpf_clients` queda normalizada al shape moderno (`reference`, `display_name`, `status`, `metadata`) para poder retirar compatibilidad runtime.
- `apps/web/lib/access-store.ts` y `apps/web/lib/client-store.ts` ya no contienen fallback legacy.
- Verificaciones del repo: `typecheck`, `lint` y `build` de `apps/web` en verde.
- E2E en verde sobre la web publica vigente el 2026-03-07: `4 passed` en `https://web-tan-mu-35.vercel.app`.
- Preview desplegada el 2026-03-07: `https://web-89q5bgnxu-moises-menendezs-projects.vercel.app`.
- La preview esta protegida por `Vercel Authentication`; se valido con `vercel curl` en `/login` y `/api/session`.
- La promocion a `production` de la web sigue siendo una accion separada: `vercel --prod`.

## Actualizacion 2026-03-07 onboarding despacho

- `apps/web/app/api/access/users/route.ts` deja de pedir password manual y emite enlaces seguros de onboarding para usuarios activos.
- `apps/web/app/api/access/users/[id]/links/route.ts` permite regenerar onboarding y reset password con persistencia de `auth_user_id` cuando faltaba el enlace fuerte.
- `apps/web/app/onboarding/page.tsx` y `apps/web/components/onboarding-workspace.tsx` consumen `access_token`/`refresh_token` del hash y materializan la sesion real para primer acceso o recovery.
- `apps/web/components/access-admin-workspace.tsx` expone estado de onboarding, copia de enlaces y reset administrado.
- `apps/web/lib/client-store.ts` vuelve a ser compatible con el esquema runtime actual seteando `full_name` al crear clientes, para no romper la suite critica.
- Verificacion local final: `typecheck`, `lint`, `build` y `playwright` de `apps/web` en verde con `5 passed`.
- Preview desplegada al cierre de esta fase: `https://web-cun07csx9-moises-menendezs-projects.vercel.app`.
- Validacion remota basica con `vercel curl`: `GET /login`, `GET /onboarding` y `GET /api/session` coherentes en preview.

## Actualizacion 2026-03-07 fase 2 slice lotes runtime

- `apps/web/lib/operations.ts` persiste ya `description`, `amount`, `currency` y `retention` en `irpf_operations`, y admite `POSICION` en runtime.
- `infra/supabase/migrations/20260307160000_irpf_lots_runtime_module.sql` introduce `irpf_lots` como tabla derivada por expediente.
- `apps/web/lib/lots.ts` recalcula lotes FIFO basicos tras cada `replaceDocumentOperations`.
- `apps/web/app/api/documents/intake/route.ts` y `apps/web/app/api/review/[extraction_id]/route.ts` regeneran lotes al persistir operaciones.
- `apps/web/app/api/expedientes/[id]/route.ts` y `apps/web/components/expediente-summary.tsx` exponen y muestran operaciones fiscales y lotes de adquisicion.
- Supabase remoto recibio tambien `infra/supabase/migrations/20260306130000_reconcile_irpf_operations.sql`, porque las columnas reconciliadas de `irpf_operations` todavia no estaban presentes en produccion.
- Verificacion ejecutada: `lint`, `build`, `typecheck` y `cd apps/web && set -a && source .env.local && set +a && npx playwright test` con `6 passed`.
- La suite anadio cobertura especifica de lotes en `apps/web/e2e/lots-runtime.spec.ts` y comprobacion de headings de expediente en `apps/web/e2e/critical-flows.spec.ts`.
- Despliegue a produccion ejecutado el 2026-03-07: `https://web-tan-mu-35.vercel.app` actualizado desde `https://web-g45hhkd5i-moises-menendezs-projects.vercel.app`.
- Validacion remota basica post-deploy con `vercel curl`: `GET /login` devuelve la pantalla de acceso y `GET /api/session` responde `{"error":"Sesión no autenticada."}` sin credenciales.

## Actualizacion 2026-03-07 fase 2 slice asignaciones FIFO

- `infra/supabase/migrations/20260307170000_irpf_sale_allocations_runtime_module.sql` introduce `irpf_sale_allocations`.
- `apps/web/lib/lots.ts` ya deriva en una sola pasada lotes, asignaciones `venta -> lote` y resumen fiscal por venta.
- `apps/web/app/api/expedientes/[id]/route.ts` expone `sale_summaries` con cantidad asignada, coste fiscal, ganancia/pérdida calculada y estado del cuadre.
- `apps/web/components/expediente-summary.tsx` añade la sección `Ganancias y pérdidas`.
- `apps/web/app/api/exports/[expediente_id]/route.ts` valida el modelo 100 con el runtime fiscal persistido, detectando ventas no cuadradas o sin coste.
- `apps/web/app/api/exports/[expediente_id]/download/route.ts` genera ya el modelo 100 desde ventas fiscalmente resumidas, no desde `realized_gain` crudo del parser.
- La descarga del modelo 100 queda bloqueada si la validacion devuelve errores; no se puede emitir un fichero AEAT con ventas no cuadradas.
- Verificacion ejecutada: `lint`, `build`, `typecheck` y `cd apps/web && set -a && source .env.local && set +a && npx playwright test` con `7 passed`.
- Despliegue a produccion actualizado el 2026-03-07: `https://web-tan-mu-35.vercel.app` desde `https://web-ceaw42msf-moises-menendezs-projects.vercel.app`.
